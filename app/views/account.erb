<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <a href="/logout">Log Out</a>
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= Helpers.current_user %></h1>
      <h3>Your balance: <%= Helpers.current_user.balance %></h3>
    <% else %>
      <a href="/">Return to the home page</a>
    <% end %>

  </body>
</html>
